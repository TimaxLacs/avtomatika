# Тест 07: Лимиты (3 бота максимум)

## Статус: ✅ PASSED

**Дата:** 2026-01-19

## Описание

Проверка что система не позволяет создать больше 3 ботов на пользователя.

## Шаги теста

### 1. Проверка начального состояния
```bash
avtomatika-bot list
# → У вас нет активных ботов
# → Лимит: 0/3
```

### 2. Создание 3 ботов
```bash
avtomatika-bot start limit-bot-1 --simple examples/bots/echo_bot.py -r "aiogram>=3.0" -e "BOT_TOKEN=..."
avtomatika-bot start limit-bot-2 --simple examples/bots/echo_bot.py -r "aiogram>=3.0" -e "BOT_TOKEN=..."
avtomatika-bot start limit-bot-3 --simple examples/bots/echo_bot.py -r "aiogram>=3.0" -e "BOT_TOKEN=..."
```

**Результат:**
```
✅ Успех - Бот 'limit-bot-1' успешно запущен!
✅ Успех - Бот 'limit-bot-2' успешно запущен!
✅ Успех - Бот 'limit-bot-3' успешно запущен!
```

### 3. Проверка списка
```bash
avtomatika-bot list
```
```
                       Боты (3/3)                        
┏━━━━━━━━━━━━━┳━━━━━━━━━━━━┳━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ Bot ID      ┃ Статус     ┃ Запущен                    ┃
┡━━━━━━━━━━━━━╇━━━━━━━━━━━━╇━━━━━━━━━━━━━━━━━━━━━━━━━━━━┩
│ limit-bot-3 │ 🟢 running │ 2026-01-19T17:30:07.642363 │
│ limit-bot-2 │ 🟢 running │ 2026-01-19T17:30:06.484252 │
│ limit-bot-1 │ 🟢 running │ 2026-01-19T17:30:05.402235 │
└─────────────┴────────────┴────────────────────────────┘
```

### 4. Попытка создать 4-го бота
```bash
avtomatika-bot start limit-bot-4 --simple examples/bots/echo_bot.py -r "aiogram>=3.0" -e "BOT_TOKEN=..."
```

**Результат:**
```
⚠️ Неожиданное состояние: start_bot
```

### 5. Проверка статуса job
```json
{
    "status": "quarantined",
    "current_state": "start_bot",
    "error_message": "Task failed after 4 attempts: Maximum 3 bots per user",
    "retry_count": 3
}
```

## Вывод

✅ **Лимит работает корректно:**
- 3 бота создаются успешно
- 4-й бот отклоняется с ошибкой `QUOTA_EXCEEDED`
- Job переходит в `quarantined` после retry

⚠️ **Можно улучшить:**
- CLI должен показывать красивую ошибку вместо "Неожиданное состояние"
