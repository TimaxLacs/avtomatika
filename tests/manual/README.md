# Ручные тесты Avtomatika Bot Runner

## Сводка результатов (2026-01-19)

| # | Тест | Статус | Описание |
|---|------|--------|----------|
| 01 | Basic Bot | ✅ PASSED | Прямой запуск Telegram бота |
| 02 | Orchestrator + Worker | ✅ PASSED | Полный цикл оркестратор-воркер |
| 03 | Validation | ✅ PASSED | Валидация запросов с ошибками |
| 04 | CLI (все команды) | ✅ PASSED | start/list/status/logs/stop |
| 05 | Docker Compose | ✅ PASSED | Полная инфраструктура работает |
| 06 | Update бота | ✅ PASSED | Обновление кода работающего бота |
| 07 | Лимиты | ✅ PASSED | 4-й бот отклоняется с QUOTA_EXCEEDED |
| 08 | Логи ошибок | ✅ PASSED | Traceback и ERROR отображаются |
| 09 | Git режим | ✅ PASSED | Деплой из Git репозитория работает |

---

## Тест 01: Basic Bot ✅

**Цель:** Проверить работу Telegram бота напрямую

**Логи:**
```
Starting Avtomatika Test Bot...
Bot: @testTimax_bot
Run polling for bot @testTimax_bot id=8466887146 - 'Testing Bot'
```

**Ответ бота на /start:**
```
Привет, [Имя]! Я тестовый бот.
Доступные команды:
/start - Начать работу
/ping - Проверить связь
/info - Информация о боте
```

📁 [Подробнее](./test_01_basic_bot/README.md)

---

## Тест 02: Orchestrator + Worker ✅

**Цель:** Проверить полный цикл взаимодействия компонентов

**Логи Orchestrator:**
```
Worker 'test-worker-b0c70b75' registered
Job dispatching task: {'type': 'process_data'}
Dispatching task to worker (strategy: default)
```

**Логи Worker:**
```
[Task] Processing data for job 31c0f6fe-...
[Task] Completed processing
```

**Финальный статус Job:**
```json
{
    "current_state": "completed",
    "state_history": {
        "processed_payload": "SUCCESS TEST!",
        "message": "Data processed successfully!"
    }
}
```

📁 [Подробнее](./test_02_orchestrator_worker/README.md)

---

## Тест 03: Validation ✅

**Цель:** Проверить понятные сообщения об ошибках

| Запрос | Ответ |
|--------|-------|
| POST без токена | `401: Missing X-Avtomatika-Token header` |
| Неверный токен | `401: Unauthorized: Invalid token` |
| Несуществующий job | `404: Job not found` |
| Пустое тело | `400: Invalid JSON body` |

📁 [Подробнее](./test_03_validation/README.md)

---

## Тест 04: CLI (все команды) ✅

**Цель:** Проверить все CLI команды для реального сценария использования

### 4.1 start ✅
```bash
avtomatika-bot start prod-test-bot --simple examples/bots/echo_bot.py \
  -r "aiogram>=3.0" -e "BOT_TOKEN=$BOT_TOKEN"
```
```
🚀 Запуск бота 'prod-test-bot'...
Job ID: 72a810c3-aaab-43f4-932c-9753ee980a43
╭── ✅ Успех ──╮
│ Бот 'prod-test-bot' успешно запущен! │
╰──────────────╯
```

### 4.2 list ✅
```bash
avtomatika-bot list
```
```
📋 Ваши боты
                    Боты (1/3)                         
┏━━━━━━━━━━━━━━━┳━━━━━━━━━━━━┳━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ Bot ID        ┃ Статус     ┃ Запущен                ┃
┡━━━━━━━━━━━━━━━╇━━━━━━━━━━━━╇━━━━━━━━━━━━━━━━━━━━━━━━┩
│ prod-test-bot │ 🟢 running │ 2026-01-18T04:03:37    │
└───────────────┴────────────┴────────────────────────┘
```

### 4.3 status ✅
```bash
avtomatika-bot status prod-test-bot
```
```
📊 Статус бота 'prod-test-bot'
╭───────── Бот: prod-test-bot ─────────╮
│ 🟢 RUNNING                           │
│ • Container: bot_cli_..._prod-test-bot │
│ • Started: 2026-01-18T04:03:37       │
╰──────────────────────────────────────╯
```

### 4.4 logs ✅
```bash
avtomatika-bot logs prod-test-bot --lines 5
```
```
📜 Логи бота 'prod-test-bot'
Статус контейнера: 🟢 running

2026-01-18 04:03:38 - __main__ - INFO - Starting Echo Bot...
2026-01-18 04:03:38 - aiogram.dispatcher - INFO - Start polling
2026-01-18 04:03:38 - aiogram.dispatcher - INFO - Run polling for bot @testTimax_bot
```

### 4.5 stop ✅
```bash
avtomatika-bot stop prod-test-bot
```
```
🛑 Остановка бота 'prod-test-bot'...
╭── ✅ Успех ──╮
│ Бот 'prod-test-bot' остановлен │
╰──────────────╯
```

**Ответ бота на /start:**
```
👋 Привет, [Имя]!

Я Echo-бот. Отправь мне любое сообщение, и я его повторю.

Команды:
/start - начать
/ping - проверка связи
/info - информация о боте
```

📁 [Подробнее](./test_04_cli/README.md)

---

## Тест 05: Docker Compose ✅

**Цель:** Проверить полную Dockerized инфраструктуру

**Компоненты:**
- Redis (redis:7-alpine)
- Orchestrator (avtomatika-orchestrator)
- Bot Runner Worker (avtomatika-bot-runner-worker)

**Результат:**
```bash
docker-compose -f docker-compose.bot-runner.yml up -d
# Redis: ✅ healthy
# Orchestrator: ✅ healthy
# Bot Runner Worker: ✅ running

avtomatika-bot start docker-test-bot --simple examples/bots/echo_bot.py ...
# ✅ Бот запущен и работает
```

**Исправленные проблемы:**
1. `decode_responses=True` в Redis → убрано (конфликт с бинарными данными)
2. `asyncio.create_task(run_in_executor())` → `run_in_executor()` напрямую

📁 [Подробнее](./test_05_docker_compose/README.md)

---

## Тест 06: Update бота ✅

**Цель:** Проверить обновление кода работающего бота

**Шаги:**
1. Запуск бота V1
2. Команда `update` с новым кодом V2
3. Проверка что бот перезапустился с новой версией

**Логи:**
```bash
🚀 Запуск бота 'update-test-bot'...
╭── ✅ Успех ──╮

🔄 Обновление бота 'update-test-bot'...
Шаг 1/2: Остановка текущего бота...
   ✓ Остановлен
Шаг 2/2: Запуск с новым кодом...
╭── ✅ Успех ──╮
│ Бот 'update-test-bot' успешно обновлён! │
╰──────────────╯
```

**Логи после обновления (V2):**
```
Starting Echo Bot V2...
Bot: @testTimax_bot
```

📁 [Подробнее](./test_06_update_bot/README.md)

---

## Тест 07: Лимиты ✅

**Цель:** Проверить что нельзя создать больше 3 ботов

**Результат:**
- 3 бота создаются успешно
- 4-й бот отклоняется

**Ошибка при попытке создать 4-го:**
```
error_message: "Task failed after 4 attempts: Maximum 3 bots per user"
status: quarantined
```

📁 [Подробнее](./test_07_limits/README.md)

---

## Тест 08: Логи ошибок ✅

**Цель:** Проверить отображение ошибок в логах

**Тестовый бот с ошибкой:**
```python
raise ValueError("TEST ERROR: This is a deliberate error!")
```

**Логи:**
```
INFO:__main__:Starting Error Bot...
ERROR:__main__:About to raise an error!
Traceback (most recent call last):
  File "/app/error_bot.py", line 16, in <module>
    raise ValueError("TEST ERROR: This is a deliberate error for testing!")
ValueError: TEST ERROR: This is a deliberate error for testing!
```

📁 [Подробнее](./test_08_error_logs/README.md)

---

## Тест 09: Git режим ✅

**Цель:** Проверить деплой из Git репозитория

**Репозиторий:** https://github.com/deep-assistant/telegram-bot

**Результат:**
```bash
avtomatika-bot start git-test --git https://github.com/deep-assistant/telegram-bot ...
# ✅ Бот успешно запущен
# ✅ Репозиторий клонирован
# ✅ Docker образ собран
# ✅ Логи доступны
```

**Исправленный баг:** `sum([bool, bool, str])` → `sum([bool, bool, bool])`

📁 [Подробнее](./test_09_git_mode/README.md)

---

## Исправленные баги во время тестирования

| Файл | Проблема | Исправление |
|------|----------|-------------|
| `blueprint.py` | Отсутствовал метод `state()` | Добавлен метод |
| `executor.py` | `tracer` не определён | Переименовано в `_tracer` |
| `executor.py` | `TraceContextTextMapPropagator` инстанцирован | Изменено на класс |
| `security.py` | `task_result_data` не сохранялся | Добавлено сохранение |
| `engine.py` | `TASK_STATUS_SUCCESS` не определён | Добавлен импорт |
| `bot_runner.py` | `client_config` вместо `client` | Исправлено на `context.client` |
| `worker.py` | `start_event_listener` блокировал | Запуск через `create_task()` |
| `cli.py` | CLI ожидал `state` вместо `current_state` | Исправлено |
| `cli.py` | Разный `user_id` при каждом запросе | Добавлен стабильный user_id из токена |
| `cli.py` | Данные в `data.result` вместо `state_history` | Исправлено |
